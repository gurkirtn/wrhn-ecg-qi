# Synthetic sample data

The image files in this directory are synthetic placeholders for interface and
pipeline development. They are not diagnostic ECGs and contain no patient data.

Never add real patient identifiers, exported clinical records, or unapproved
datasets to this repository.
